spring:
  profiles:
    include:
      - oauth
  datasource:
    url: jdbc:mysql://localhost:3306/lookfit_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 651212
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
      encoding: UTF-8
      continue-on-error: true